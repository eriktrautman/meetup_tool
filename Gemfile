source 'https://rubygems.org'

gem 'typhoeus'

# This uses a local version of the gem because the
# rubygems one doesn't actually reflect the most
# recent changes to the repo, so I had to download
# it and manually build the gem with `rake build`
# then make sure this path points to it in my directory.
# BLEH!
gem 'rMeetup', '>=1.0', :path => '../rmeetup'
gem 'pry-byebug'